import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#d3fd60'
        },
        secondary: {
            main: '#dc9009'
        },
        info: {
            main: '#60d3fd'
        }

    }
})